# Disable caching for the current session

Disable caching for the current session

## Usage

``` r
tw_disable_cache()
```

## Value

Nothing, used for its side effects.

## Examples

``` r
# \donttest{
if (interactive()) {
  tw_disable_cache()
}
# }
```
